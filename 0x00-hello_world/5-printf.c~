#include <stdio.h>
/* This file prints a string to the standard output */
/**
 * main - Prints string to standard output
 *
 * Return: 0
 */
int main(void)
{
  printf("%s\n", "with proper grammar, but the outcome is a piece of art,");

  return(0);
}
